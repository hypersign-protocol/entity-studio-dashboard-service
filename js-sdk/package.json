{
  "name": "entity-studio-js-sdk",
  "version": "1.0.2",
  "description": "",
  "scripts": {
    "dev": "parcel build index.js --dist-dir build  --no-source-maps && node app.js",
    "build": "npm run pretty && npm run lint && parcel build index.js --dist-dir build",
    "lint": "eslint index.js",
    "pretty": "prettier --config .prettierrc.json --write index.js ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm run build && cp dist/index.js build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "buffer": "^6.0.3",
    "eslint": "^8.3.0",
    "parcel": "^2.0.1",
    "prettier": "^2.5.0"
  },
  "dependencies": {
    "axios": "^1.1.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.2.10",
    "path": "^0.12.7",
    "qrcode": "^1.5.0"
  }
}
